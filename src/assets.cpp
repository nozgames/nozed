//
// Auto-generated asset manifest - DO NOT EDIT MANUALLY
// Generated by NoZ Game Engine Asset Importer
//

// @includes
#include <noz/noz.h>
#include "assets.h"

// @assets
LoadedAssets Assets = {};

// @names
// Static asset names for efficient comparison and safer asset management
static const Name* NAME_fonts_roboto_black;
static const Name* NAME_shaders_default;
static const Name* NAME_shaders_text;
static const Name* NAME_shaders_ui;
static const Name* NAME_shaders_vfx;
static const Name* NAME_textures_palette;
static const Name* NAME_ui_mesh_editor;

#ifdef NOZ_EDITOR

void HotloadAsset(const Name* incoming_name)
{
    // @fonts
    NOZ_RELOAD_FONT(NAME_fonts_roboto_black, Assets.fonts.roboto_black);

    // @shaders
    NOZ_RELOAD_SHADER(NAME_shaders_default, Assets.shaders._default);
    NOZ_RELOAD_SHADER(NAME_shaders_text, Assets.shaders.text);
    NOZ_RELOAD_SHADER(NAME_shaders_ui, Assets.shaders.ui);
    NOZ_RELOAD_SHADER(NAME_shaders_vfx, Assets.shaders.vfx);

    // @stylesheets
    NOZ_RELOAD_STYLE_SHEET(NAME_ui_mesh_editor, Assets.ui.mesh_editor);

    // @textures
    NOZ_RELOAD_TEXTURE(NAME_textures_palette, Assets.textures.palette);

}
#endif // NOZ_EDITOR

// @init
bool LoadAssets(Allocator* allocator)
{
    // Initialize static name_t variables for asset management
    NAME_fonts_roboto_black = GetName("fonts/Roboto-Black");
    NAME_shaders_default = GetName("shaders/default");
    NAME_shaders_text = GetName("shaders/text");
    NAME_shaders_ui = GetName("shaders/ui");
    NAME_shaders_vfx = GetName("shaders/vfx");
    NAME_textures_palette = GetName("textures/palette");
    NAME_ui_mesh_editor = GetName("ui/mesh_editor");

    NOZ_LOAD_FONT(allocator, NAME_fonts_roboto_black, Assets.fonts.roboto_black);
    NOZ_LOAD_SHADER(allocator, NAME_shaders_default, Assets.shaders._default);
    NOZ_LOAD_SHADER(allocator, NAME_shaders_text, Assets.shaders.text);
    NOZ_LOAD_SHADER(allocator, NAME_shaders_ui, Assets.shaders.ui);
    NOZ_LOAD_SHADER(allocator, NAME_shaders_vfx, Assets.shaders.vfx);
    NOZ_LOAD_TEXTURE(allocator, NAME_textures_palette, Assets.textures.palette);
    NOZ_LOAD_STYLE_SHEET(allocator, NAME_ui_mesh_editor, Assets.ui.mesh_editor);

    // Assign core engine assets
    g_core_assets.shaders.ui = Assets.shaders.ui;
    g_core_assets.shaders.text = Assets.shaders.text;
    g_core_assets.shaders.vfx = Assets.shaders.vfx;
    g_core_assets.fonts.fallback = Assets.fonts.roboto_black;

    return true;
}

// @uninit
void UnloadAssets()
{
    // Clear all asset pointers
    memset(&Assets, 0, sizeof(Assets));
}
